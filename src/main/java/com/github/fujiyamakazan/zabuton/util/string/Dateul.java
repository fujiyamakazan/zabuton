package com.github.fujiyamakazan.zabuton.util.string;

import com.github.fujiyamakazan.zabuton.util.date.Chronus;

/**
 * Date型のユーティリティです。
 * [Date]-[U]ti[l]s
 *
 * 時間に関するユーティリティとして「Chronus」を定義していますが、
 * スペルが難しく、スムーズにコードアシストできない事があるため、
 * フック用として作成しました。
 *
 */
public class Dateul extends Chronus {
    private static final long serialVersionUID = 1L;
    @SuppressWarnings("unused")
    private static final org.slf4j.Logger LOGGER = org.slf4j.LoggerFactory
        .getLogger(java.lang.invoke.MethodHandles.lookup().lookupClass());

}
